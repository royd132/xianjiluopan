# 评审问答与提交检查表

## 常见追问

### 为什么一定要多 Agent？

不同任务具有不同的输入、验证方式和失败模式：原语评论需要语义模型，价格与趋势需要确定性统计，供应链需要阈值监控，最终建议需要 Schema 编译与安全校验。将它们拆为专业 Agent，可以并行执行、独立评测、单点降级，并通过黑板追踪每个结论的来源。

### 这是不是只有多 Agent 的概念图？

不是。仓库已经包含六类 Agent、CollaborationBlackboard、事件协议、阶段并行 Runtime、Trace、SQLite Memory、Checkpoint、四卡评测闸门、FastAPI/SSE、CLI 和自动化测试。默认离线模式可以直接生成完整报告。

### Harness 有什么实际作用？

Harness 位于 Agent 外层，统一记录开始、完成和异常，限制工具，保存任务快照并写入长期记忆。它解决的是 Agent 能否被恢复、审计和评测的问题，而不是增加一个新的聊天角色。

### Mock 数据是不是造假？

Mock 模式在页面、API 和报告中明确标记，目的在于让评审无网络、无密钥也能复现完整系统。数据 Provider 接口独立，真实接入不会改变 Agent 工作流。项目不会把 Mock 结果描述为真实实时发现。

### 如何控制模型幻觉？

模型不负责精确数值计算。Decision Compiler 只能读取结构化事实；Safety Evaluator 强制检查证据数量、非英语来源、私域钩子和失效条件。缺少必填字段时不发布最终卡片。

### 自进化是否已经实现？

反馈收集和正负案例沉淀已经实现。SFT、LoRA 与 Agentic RL 是累积足够合规反馈后的 Layer 3 路线，明确标记为后续能力。

### 与传统选品工具的差异？

传统工具偏重指标和榜单；先机罗盘输出可执行四卡，并提供原语痛点、私域落点、证据链、反向条件、运行审计和人工反馈闭环。它也可以把传统数据工具作为上游数据源，而不是简单替代它们。

## 提交检查

- [x] 填写团队与队长信息；
- [x] 参赛场景统一为“场景三：AI 市场洞察”；
- [ ] README、视频和表单统一使用四卡口径；
- [ ] 展示多 Agent Runtime、Harness 和自进化三层路线；
- [ ] 清楚区分已实现 Layer 1/2 与规划中的 Layer 3；
- [ ] 准备工作台、四卡、原语痛点、证据链、Trace/Checkpoint 截图；
- [ ] 提供仓库和可访问 Demo；
- [ ] 清除 API Key、账号、个人隐私和本地绝对路径；
- [ ] 在干净环境运行后端测试与前端构建；
- [ ] 确认 Mock 数据标识可见。
